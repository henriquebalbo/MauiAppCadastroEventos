<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiAppEventos.Views.CadastroEvento"
             Title="** Cadastro de Evento **"
             Background="Olive">
    

    <ScrollView>

        <VerticalStackLayout Padding="10" Spacing="10">

            <Entry Placeholder="Nome do Evento" x:Name="txtNome" TextColor="GreenYellow" FontSize="20"/>
            <DatePicker x:Name="dtInicio" 
                   Format="dd/MM/yyyy" TextColor="White" />
            <DatePicker x:Name="dtTermino" 
                   Format="dd/MM/yyyy" TextColor="White"/>
            <Entry Placeholder="Número de Participantes" Keyboard="Numeric" x:Name="txtParticipantes" />
            <Entry Placeholder="Local do Evento" x:Name="txtLocal" />
            <Entry Placeholder="Custo por Participante (R$)" Keyboard="Numeric" x:Name="txtCusto" />

            <Button Text="Cadastrar Evento"
                    BackgroundColor="AliceBlue"
                    TextColor="Blue"
                    Clicked="OnCadastrarClicked"/>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
